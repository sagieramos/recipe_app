<p style="color: green"><%= notice %></p>

<%= render @inventory %>


<div>
  <%= link_to "Add food", new_inventory_inventory_food_path(@inventory) %> |
  <%= link_to "Back to inventories", inventories_path %>

  <%= button_to "Destroy this inventory", @inventory, method: :delete %>
</div>

<%= render partial:'inventory_foods/inventory_food', collection: @inventory.foods, as: :food, locals:{inventory: @inventory} %>
